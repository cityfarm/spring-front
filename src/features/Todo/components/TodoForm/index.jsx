import React from 'react'
import PropTypes from 'prop-types'

function TodoForm(props) {
  return (
    <div>Todo Form</div>
  )
}

TodoForm.propTypes = {
    onSunmit: PropTypes.func,
}

export default TodoForm
